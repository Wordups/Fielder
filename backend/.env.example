# Fielder onboarding extractor — private backend only (never commit a real key).
# Get a key at https://console.anthropic.com/ . The static pages call /api/extract;
# this is where the Claude vision call (and the key) live.
ANTHROPIC_API_KEY=
